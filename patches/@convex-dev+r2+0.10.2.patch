diff --git a/node_modules/@convex-dev/r2/dist/client/index.d.ts b/node_modules/@convex-dev/r2/dist/client/index.d.ts
index 0bddfaf..65a27b6 100644
--- a/node_modules/@convex-dev/r2/dist/client/index.d.ts
+++ b/node_modules/@convex-dev/r2/dist/client/index.d.ts
@@ -14,6 +14,45 @@ export declare const DEFAULT_BATCH_SIZE = 10;
 export type ClientApi = ApiFromModules<{
     client: ReturnType<R2["clientApi"]>;
 }>["client"];
+export type R2Metadata = {
+    authorId?: string;
+    bucket: string;
+    bucketLink: string;
+    contentType?: string;
+    key: string;
+    lastModified: string;
+    link: string;
+    sha256?: string;
+    size?: number;
+    uploadStatus?: "pending" | "ready";
+    expectedSize?: number;
+    expectedContentType?: string;
+    uploadExpiresAt?: number;
+    url: string;
+};
+export type DirectUploadOptions = {
+    contentLength: number;
+    contentType: string;
+    expiresIn?: number;
+    ifNoneMatch?: string;
+};
+export type ReservedDirectUploadOptions = DirectUploadOptions & {
+    key: string;
+    authorId: string;
+};
+export type DirectUpload = {
+    key: string;
+    url: string;
+    headers: Record<string, string>;
+    expiresAt: number;
+};
+export type R2MetadataPage = {
+    continueCursor: string;
+    isDone: boolean;
+    page: R2Metadata[];
+    pageStatus?: null | "SplitRecommended" | "SplitRequired";
+    splitCursor?: null | string;
+};
 type QueryCtx = Pick<GenericQueryCtx<GenericDataModel>, "runQuery">;
 type MutationCtx = Pick<GenericMutationCtx<GenericDataModel>, "runQuery" | "runMutation">;
 type ActionCtx = Pick<GenericActionCtx<GenericDataModel>, "runQuery" | "runMutation" | "runAction">;
@@ -32,6 +71,8 @@ export declare class R2 {
         R2_ACCESS_KEY_ID?: string;
         /** @deprecated Use `secretAccessKey` instead. */
         R2_SECRET_ACCESS_KEY?: string;
+        forcePathStyle?: boolean;
+        R2_FORCE_PATH_STYLE?: boolean;
         defaultBatchSize?: number;
     };
     readonly config: Infer<typeof r2ConfigValidator>;
@@ -81,6 +122,8 @@ export declare class R2 {
         R2_ACCESS_KEY_ID?: string;
         /** @deprecated Use `secretAccessKey` instead. */
         R2_SECRET_ACCESS_KEY?: string;
+        forcePathStyle?: boolean;
+        R2_FORCE_PATH_STYLE?: boolean;
         defaultBatchSize?: number;
     });
     /**
@@ -102,10 +145,8 @@ export declare class R2 {
      *   - `key` - The R2 object key.
      *   - `url` - A signed URL for uploading the object.
      */
-    generateUploadUrl(customKey?: string): Promise<{
-        key: string;
-        url: string;
-    }>;
+    generateUploadUrl(customKey?: string, options?: Partial<DirectUploadOptions>): Promise<DirectUpload>;
+    createDirectUpload(ctx: MutationCtx | ActionCtx, options: ReservedDirectUploadOptions): Promise<DirectUpload>;
     /**
      * Store a blob in R2 and sync the metadata to Convex.
      *
@@ -123,6 +164,7 @@ export declare class R2 {
         type?: string;
         disposition?: string;
         cacheControl?: string;
+        authorId?: string;
     }): Promise<string>;
     /**
      * Retrieve R2 object metadata and store in Convex.
@@ -131,7 +173,10 @@ export declare class R2 {
      * @param key - The R2 object key.
      * @returns A promise that resolves when the metadata is synced.
      */
-    syncMetadata(ctx: ActionCtx, key: string): Promise<void>;
+    syncMetadata(ctx: ActionCtx, key: string, options?: {
+        authorId?: string;
+        requireReservation?: boolean;
+    }): Promise<void>;
     /**
      * Retrieve R2 object metadata from Convex.
      *
@@ -139,17 +184,7 @@ export declare class R2 {
      * @param key - The R2 object key.
      * @returns A promise that resolves to the metadata for the object.
      */
-    getMetadata(ctx: QueryCtx | MutationCtx | ActionCtx, key: string): Promise<{
-        bucket: string;
-        bucketLink: string;
-        contentType?: string;
-        key: string;
-        lastModified: string;
-        link: string;
-        sha256?: string;
-        size?: number;
-        url: string;
-    } | null>;
+    getMetadata(ctx: QueryCtx | MutationCtx | ActionCtx, key: string): Promise<R2Metadata | null>;
     /**
      * Retrieve all metadata from Convex for a given bucket.
      *
@@ -157,23 +192,8 @@ export declare class R2 {
      * @param limit (optional) - The maximum number of documents to return.
      * @returns A promise that resolves to an array of metadata documents.
      */
-    listMetadata(ctx: QueryCtx | MutationCtx | ActionCtx, limit?: number, cursor?: string | null): Promise<{
-        continueCursor: string;
-        isDone: boolean;
-        page: Array<{
-            bucket: string;
-            bucketLink: string;
-            contentType?: string;
-            key: string;
-            lastModified: string;
-            link: string;
-            sha256?: string;
-            size?: number;
-            url: string;
-        }>;
-        pageStatus?: null | "SplitRecommended" | "SplitRequired";
-        splitCursor?: null | string;
-    }>;
+    listMetadata(ctx: QueryCtx | MutationCtx | ActionCtx, limit?: number, cursor?: string | null): Promise<R2MetadataPage>;
+    listMetadata(ctx: QueryCtx | MutationCtx | ActionCtx, authorId?: string, limit?: number, cursor?: string | null, keyPrefix?: string): Promise<R2MetadataPage>;
     /**
      * Delete an object from R2.
      *
@@ -248,17 +268,7 @@ export declare class R2 {
          */
         getMetadata: import("convex/server").RegisteredQuery<"public", {
             key: string;
-        }, Promise<{
-            bucket: string;
-            bucketLink: string;
-            contentType?: string;
-            key: string;
-            lastModified: string;
-            link: string;
-            sha256?: string;
-            size?: number;
-            url: string;
-        } | null>>;
+        }, Promise<R2Metadata | null>>;
         /**
          * Retrieve all metadata for a given bucket from Convex.
          */
@@ -271,23 +281,7 @@ export declare class R2 {
                 numItems: number;
                 cursor: string | null;
             };
-        }, Promise<{
-            continueCursor: string;
-            isDone: boolean;
-            page: Array<{
-                bucket: string;
-                bucketLink: string;
-                contentType?: string;
-                key: string;
-                lastModified: string;
-                link: string;
-                sha256?: string;
-                size?: number;
-                url: string;
-            }>;
-            pageStatus?: null | "SplitRecommended" | "SplitRequired";
-            splitCursor?: null | string;
-        }>>;
+        }, Promise<R2MetadataPage>>;
         /**
          * Delete an object from R2 and remove its metadata from Convex.
          */
@@ -297,4 +291,4 @@ export declare class R2 {
     };
 }
 export {};
-//# sourceMappingURL=index.d.ts.map
\ No newline at end of file
+//# sourceMappingURL=index.d.ts.map
diff --git a/node_modules/@convex-dev/r2/dist/client/index.js b/node_modules/@convex-dev/r2/dist/client/index.js
index eb0dd22..4c36a40 100644
--- a/node_modules/@convex-dev/r2/dist/client/index.js
+++ b/node_modules/@convex-dev/r2/dist/client/index.js
@@ -13,11 +13,18 @@ const parseConfig = (config) => {
         endpoint: "R2_ENDPOINT",
         accessKeyId: "R2_ACCESS_KEY_ID",
         secretAccessKey: "R2_SECRET_ACCESS_KEY",
+        forcePathStyle: "R2_FORCE_PATH_STYLE",
     };
-    const missingFields = Object.keys(envVarNames).filter((key) => !config[key]);
+    const requiredEnvVarNames = {
+        bucket: envVarNames.bucket,
+        endpoint: envVarNames.endpoint,
+        accessKeyId: envVarNames.accessKeyId,
+        secretAccessKey: envVarNames.secretAccessKey,
+    };
+    const missingFields = Object.keys(requiredEnvVarNames).filter((key) => !config[key]);
     if (missingFields.length > 0) {
         throw new Error(`R2 configuration is missing required fields: ${missingFields.join(", ")}\n` +
-            `Set them via environment variables (${missingFields.map((key) => envVarNames[key]).join(", ")}) ` +
+            `Set them via environment variables (${missingFields.map((key) => requiredEnvVarNames[key]).join(", ")}) ` +
             `or pass them as options to the R2 constructor.`);
     }
     return config;
@@ -96,6 +103,9 @@ export class R2 {
             secretAccessKey: options?.secretAccessKey ??
                 options?.R2_SECRET_ACCESS_KEY ??
                 process.env.R2_SECRET_ACCESS_KEY,
+            forcePathStyle: options?.forcePathStyle ??
+                options?.R2_FORCE_PATH_STYLE ??
+                process.env.R2_FORCE_PATH_STYLE === "true",
         };
     }
     /**
@@ -118,10 +128,42 @@ export class R2 {
      *   - `key` - The R2 object key.
      *   - `url` - A signed URL for uploading the object.
      */
-    async generateUploadUrl(customKey) {
+    async generateUploadUrl(customKey, options = {}) {
         const key = customKey || crypto.randomUUID();
-        const url = await getSignedUrl(this.client, new PutObjectCommand({ Bucket: this.config.bucket, Key: key }));
-        return { key, url };
+        const { contentLength, contentType, expiresIn = 900, ifNoneMatch } = options;
+        const headers = {};
+        const signableHeaders = new Set();
+        if (contentType) {
+            headers["Content-Type"] = contentType;
+            signableHeaders.add("content-type");
+        }
+        if (ifNoneMatch) {
+            headers["If-None-Match"] = ifNoneMatch;
+            signableHeaders.add("if-none-match");
+        }
+        const url = await getSignedUrl(this.client, new PutObjectCommand({
+            Bucket: this.config.bucket,
+            Key: key,
+            ContentLength: contentLength,
+            ContentType: contentType,
+            IfNoneMatch: ifNoneMatch,
+        }), {
+            expiresIn,
+            signableHeaders,
+        });
+        return { key, url, headers, expiresAt: Date.now() + expiresIn * 1000 };
+    }
+    async createDirectUpload(ctx, options) {
+        const upload = await this.generateUploadUrl(options.key, options);
+        await ctx.runMutation(this.component.lib.reserveUpload, {
+            key: upload.key,
+            authorId: options.authorId,
+            expectedSize: options.contentLength,
+            expectedContentType: options.contentType,
+            uploadExpiresAt: upload.expiresAt,
+            ...this.config,
+        });
+        return upload;
     }
     /**
      * Store a blob in R2 and sync the metadata to Convex.
@@ -162,6 +204,7 @@ export class R2 {
         await this.client.send(command);
         await ctx.runAction(this.component.lib.syncMetadata, {
             key: key,
+            authorId: opts.authorId,
             ...this.config,
         });
         return key;
@@ -173,9 +216,11 @@ export class R2 {
      * @param key - The R2 object key.
      * @returns A promise that resolves when the metadata is synced.
      */
-    async syncMetadata(ctx, key) {
+    async syncMetadata(ctx, key, options = {}) {
         await ctx.runAction(this.component.lib.syncMetadata, {
             key: key,
+            authorId: options.authorId,
+            requireReservation: options.requireReservation,
             ...this.config,
         });
     }
@@ -192,18 +237,22 @@ export class R2 {
             ...this.config,
         });
     }
-    /**
-     * Retrieve all metadata from Convex for a given bucket.
-     *
-     * @param ctx - A Convex query context.
-     * @param limit (optional) - The maximum number of documents to return.
-     * @returns A promise that resolves to an array of metadata documents.
-     */
-    async listMetadata(ctx, limit, cursor) {
+    async listMetadata(ctx, authorIdOrLimit, limitOrCursor, cursorOrKeyPrefix, keyPrefix) {
+        const legacySignature = typeof authorIdOrLimit === "string";
+        const authorId = legacySignature ? authorIdOrLimit : undefined;
+        const limit = legacySignature
+            ? limitOrCursor
+            : authorIdOrLimit;
+        const cursor = legacySignature
+            ? cursorOrKeyPrefix
+            : limitOrCursor;
+        const resolvedKeyPrefix = legacySignature ? keyPrefix : undefined;
         return ctx.runQuery(this.component.lib.listMetadata, {
             ...this.config,
+            authorId,
             limit: limit,
             cursor: cursor ?? undefined,
+            keyPrefix: resolvedKeyPrefix,
         });
     }
     /**
@@ -263,7 +312,8 @@ export class R2 {
                     if (opts?.checkUpload) {
                         await opts.checkUpload(ctx, this.config.bucket);
                     }
-                    return this.generateUploadUrl();
+                    const { key, url } = await this.generateUploadUrl();
+                    return { key, url };
                 },
             }),
             /**
@@ -367,4 +417,4 @@ export class R2 {
         };
     }
 }
-//# sourceMappingURL=index.js.map
\ No newline at end of file
+//# sourceMappingURL=index.js.map
diff --git a/node_modules/@convex-dev/r2/dist/component/_generated/component.d.ts b/node_modules/@convex-dev/r2/dist/component/_generated/component.d.ts
index b6a5900..53fef32 100644
--- a/node_modules/@convex-dev/r2/dist/component/_generated/component.d.ts
+++ b/node_modules/@convex-dev/r2/dist/component/_generated/component.d.ts
@@ -20,11 +20,26 @@ import type { FunctionReference } from "convex/server";
  */
 export type ComponentApi<Name extends string | undefined = string | undefined> = {
     lib: {
+        deletePendingMetadata: FunctionReference<"mutation", "internal", {
+            authorId: string;
+            bucket: string;
+            key: string;
+        }, boolean, Name>;
+        expireUpload: FunctionReference<"action", "internal", {
+            forcePathStyle?: boolean;
+            accessKeyId: string;
+            authorId: string;
+            bucket: string;
+            endpoint: string;
+            key: string;
+            secretAccessKey: string;
+        }, null, Name>;
         deleteMetadata: FunctionReference<"mutation", "internal", {
             bucket: string;
             key: string;
         }, null, Name>;
         deleteObject: FunctionReference<"mutation", "internal", {
+            forcePathStyle?: boolean;
             accessKeyId: string;
             bucket: string;
             endpoint: string;
@@ -32,6 +47,7 @@ export type ComponentApi<Name extends string | undefined = string | undefined> =
             secretAccessKey: string;
         }, null, Name>;
         deleteR2Object: FunctionReference<"action", "internal", {
+            forcePathStyle?: boolean;
             accessKeyId: string;
             bucket: string;
             endpoint: string;
@@ -39,6 +55,7 @@ export type ComponentApi<Name extends string | undefined = string | undefined> =
             secretAccessKey: string;
         }, null, Name>;
         getMetadata: FunctionReference<"query", "internal", {
+            forcePathStyle?: boolean;
             accessKeyId: string;
             bucket: string;
             endpoint: string;
@@ -48,14 +65,32 @@ export type ComponentApi<Name extends string | undefined = string | undefined> =
             bucket: string;
             bucketLink: string;
             contentType?: string;
+            authorId?: string;
             key: string;
             lastModified: string;
             link: string;
             sha256?: string;
             size?: number;
+            uploadStatus?: "pending" | "ready";
+            expectedSize?: number;
+            expectedContentType?: string;
+            uploadExpiresAt?: number;
             url: string;
         } | null, Name>;
+        getUploadReservation: FunctionReference<"query", "internal", {
+            bucket: string;
+            key: string;
+        }, {
+            authorId?: string;
+            expectedSize?: number;
+            expectedContentType?: string;
+            uploadExpiresAt?: number;
+            uploadStatus?: "pending" | "ready";
+        } | null, Name>;
         listMetadata: FunctionReference<"query", "internal", {
+            forcePathStyle?: boolean;
+            authorId?: string;
+            keyPrefix?: string;
             accessKeyId: string;
             bucket: string;
             cursor?: string;
@@ -69,24 +104,45 @@ export type ComponentApi<Name extends string | undefined = string | undefined> =
                 bucket: string;
                 bucketLink: string;
                 contentType?: string;
+                authorId?: string;
                 key: string;
                 lastModified: string;
                 link: string;
                 sha256?: string;
                 size?: number;
+                uploadStatus?: "pending" | "ready";
+                expectedSize?: number;
+                expectedContentType?: string;
+                uploadExpiresAt?: number;
                 url: string;
             }>;
             pageStatus?: null | "SplitRecommended" | "SplitRequired";
             splitCursor?: null | string;
         }, Name>;
         store: FunctionReference<"action", "internal", {
+            forcePathStyle?: boolean;
             accessKeyId: string;
             bucket: string;
             endpoint: string;
             secretAccessKey: string;
             url: string;
         }, any, Name>;
+        reserveUpload: FunctionReference<"mutation", "internal", {
+            forcePathStyle?: boolean;
+            accessKeyId: string;
+            authorId: string;
+            bucket: string;
+            endpoint: string;
+            expectedContentType: string;
+            expectedSize: number;
+            key: string;
+            secretAccessKey: string;
+            uploadExpiresAt: number;
+        }, null, Name>;
         syncMetadata: FunctionReference<"action", "internal", {
+            forcePathStyle?: boolean;
+            authorId?: string;
+            requireReservation?: boolean;
             accessKeyId: string;
             bucket: string;
             endpoint: string;
@@ -97,14 +153,20 @@ export type ComponentApi<Name extends string | undefined = string | undefined> =
         upsertMetadata: FunctionReference<"mutation", "internal", {
             bucket: string;
             contentType?: string;
+            authorId?: string;
             key: string;
             lastModified: string;
             link: string;
             sha256?: string;
             size?: number;
+            uploadStatus?: "pending" | "ready";
+            expectedSize?: number;
+            expectedContentType?: string;
+            uploadExpiresAt?: number;
+            requirePendingReservation?: boolean;
         }, {
             isNew: boolean;
         }, Name>;
     };
 };
-//# sourceMappingURL=component.d.ts.map
\ No newline at end of file
+//# sourceMappingURL=component.d.ts.map
diff --git a/node_modules/@convex-dev/r2/dist/component/lib.d.ts b/node_modules/@convex-dev/r2/dist/component/lib.d.ts
index 2eae702..1d0becc 100644
--- a/node_modules/@convex-dev/r2/dist/component/lib.d.ts
+++ b/node_modules/@convex-dev/r2/dist/component/lib.d.ts
@@ -1,12 +1,36 @@
 import type { Doc, TableNames } from "./_generated/dataModel.js";
 export declare const store: import("convex/server").RegisteredAction<"public", {
+    forcePathStyle?: boolean | undefined;
     bucket: string;
     endpoint: string;
     accessKeyId: string;
     secretAccessKey: string;
     url: string;
 }, Promise<`${string}-${string}-${string}-${string}-${string}`>>;
+export declare const reserveUpload: import("convex/server").RegisteredMutation<"public", {
+    forcePathStyle?: boolean | undefined;
+    accessKeyId: string;
+    authorId: string;
+    bucket: string;
+    endpoint: string;
+    expectedContentType: string;
+    expectedSize: number;
+    key: string;
+    secretAccessKey: string;
+    uploadExpiresAt: number;
+}, Promise<null>>;
+export declare const getUploadReservation: import("convex/server").RegisteredQuery<"public", {
+    bucket: string;
+    key: string;
+}, Promise<{
+    authorId?: string | undefined;
+    expectedSize?: number | undefined;
+    expectedContentType?: string | undefined;
+    uploadExpiresAt?: number | undefined;
+    uploadStatus?: "pending" | "ready" | undefined;
+} | null>>;
 export declare const getMetadata: import("convex/server").RegisteredQuery<"public", {
+    forcePathStyle?: boolean | undefined;
     bucket: string;
     endpoint: string;
     accessKeyId: string;
@@ -18,14 +42,22 @@ export declare const getMetadata: import("convex/server").RegisteredQuery<"publi
     bucket: string;
     sha256?: string | undefined;
     key: string;
-    contentType?: string | undefined;
-    lastModified: string;
     link: string;
+    authorId?: string | undefined;
+    contentType?: string | undefined;
     size?: number | undefined;
+    uploadStatus?: "pending" | "ready" | undefined;
+    expectedSize?: number | undefined;
+    expectedContentType?: string | undefined;
+    uploadExpiresAt?: number | undefined;
+    lastModified: string;
 } | null>>;
 export declare const listMetadata: import("convex/server").RegisteredQuery<"public", {
+    forcePathStyle?: boolean | undefined;
+    authorId?: string | undefined;
     cursor?: string | undefined;
     limit?: number | undefined;
+    keyPrefix?: string | undefined;
     bucket: string;
     endpoint: string;
     accessKeyId: string;
@@ -37,10 +69,15 @@ export declare const listMetadata: import("convex/server").RegisteredQuery<"publ
         bucket: string;
         sha256?: string | undefined;
         key: string;
-        contentType?: string | undefined;
-        lastModified: string;
         link: string;
+        authorId?: string | undefined;
+        contentType?: string | undefined;
         size?: number | undefined;
+        uploadStatus?: "pending" | "ready" | undefined;
+        expectedSize?: number | undefined;
+        expectedContentType?: string | undefined;
+        uploadExpiresAt?: number | undefined;
+        lastModified: string;
     }[];
     isDone: boolean;
     continueCursor: import("convex/server").Cursor;
@@ -49,16 +86,25 @@ export declare const listMetadata: import("convex/server").RegisteredQuery<"publ
 }>>;
 export declare const upsertMetadata: import("convex/server").RegisteredMutation<"public", {
     sha256?: string | undefined;
+    authorId?: string | undefined;
     contentType?: string | undefined;
     size?: number | undefined;
+    uploadStatus?: "pending" | "ready" | undefined;
+    expectedSize?: number | undefined;
+    expectedContentType?: string | undefined;
+    uploadExpiresAt?: number | undefined;
+    requirePendingReservation?: boolean | undefined;
     bucket: string;
     key: string;
-    lastModified: string;
     link: string;
+    lastModified: string;
 }, Promise<{
     isNew: boolean;
 }>>;
 export declare const syncMetadata: import("convex/server").RegisteredAction<"public", {
+    forcePathStyle?: boolean | undefined;
+    authorId?: string | undefined;
+    requireReservation?: boolean | undefined;
     onComplete?: string | undefined;
     bucket: string;
     endpoint: string;
@@ -66,18 +112,34 @@ export declare const syncMetadata: import("convex/server").RegisteredAction<"pub
     secretAccessKey: string;
     key: string;
 }, Promise<void>>;
+export declare const expireUpload: import("convex/server").RegisteredAction<"public", {
+    forcePathStyle?: boolean | undefined;
+    accessKeyId: string;
+    authorId: string;
+    bucket: string;
+    endpoint: string;
+    key: string;
+    secretAccessKey: string;
+}, Promise<null>>;
+export declare const deletePendingMetadata: import("convex/server").RegisteredMutation<"public", {
+    authorId: string;
+    bucket: string;
+    key: string;
+}, Promise<boolean>>;
 export declare const deleteMetadata: import("convex/server").RegisteredMutation<"public", {
     bucket: string;
     key: string;
 }, Promise<void>>;
 export declare const deleteR2Object: import("convex/server").RegisteredAction<"public", {
+    forcePathStyle?: boolean | undefined;
     bucket: string;
     endpoint: string;
     accessKeyId: string;
     secretAccessKey: string;
     key: string;
-}, Promise<void>>;
+}, Promise<null | undefined>>;
 export declare const deleteObject: import("convex/server").RegisteredMutation<"public", {
+    forcePathStyle?: boolean | undefined;
     bucket: string;
     endpoint: string;
     accessKeyId: string;
@@ -85,4 +147,4 @@ export declare const deleteObject: import("convex/server").RegisteredMutation<"p
     key: string;
 }, Promise<void>>;
 export declare const withoutSystemFields: <T extends Doc<TableNames>>(fields: T) => Omit<T, "_creationTime" | "_id">;
-//# sourceMappingURL=lib.d.ts.map
\ No newline at end of file
+//# sourceMappingURL=lib.d.ts.map
diff --git a/node_modules/@convex-dev/r2/dist/component/lib.js b/node_modules/@convex-dev/r2/dist/component/lib.js
index 88f5fa0..98cc0a9 100644
--- a/node_modules/@convex-dev/r2/dist/component/lib.js
+++ b/node_modules/@convex-dev/r2/dist/component/lib.js
@@ -9,10 +9,27 @@ import { asyncMap } from "convex-helpers";
 import { paginator } from "convex-helpers/server/pagination";
 import { ActionRetrier } from "@convex-dev/action-retrier";
 const DEFAULT_LIST_LIMIT = 100;
+const UPLOAD_CLEANUP_GRACE_MS = 5 * 60 * 1000;
 const retrier = new ActionRetrier(components.actionRetrier);
+const isNoSuchKeyError = (error) => typeof error === "object" &&
+    error !== null &&
+    ("name" in error || "Code" in error || "code" in error) &&
+    (error.name === "NoSuchKey" ||
+        error.Code === "NoSuchKey" ||
+        error.code === "NoSuchKey");
 const getUrl = async (r2, bucket, key) => {
     return await getSignedUrl(r2, new GetObjectCommand({ Bucket: bucket, Key: key }));
 };
+const deleteR2Key = async (r2, bucket, key) => {
+    try {
+        await r2.send(new DeleteObjectCommand({ Bucket: bucket, Key: key }));
+    }
+    catch (error) {
+        if (!isNoSuchKeyError(error)) {
+            throw error;
+        }
+    }
+};
 export const store = action({
     args: {
         ...r2ConfigValidator.fields,
@@ -33,6 +50,78 @@ export const store = action({
         return key;
     },
 });
+export const reserveUpload = mutation({
+    args: {
+        key: v.string(),
+        authorId: v.string(),
+        expectedSize: v.number(),
+        expectedContentType: v.string(),
+        uploadExpiresAt: v.number(),
+        ...r2ConfigValidator.fields,
+    },
+    returns: v.null(),
+    handler: async (ctx, args) => {
+        const existing = await ctx.db
+            .query("metadata")
+            .withIndex("bucket_key", (q) => q.eq("bucket", args.bucket).eq("key", args.key))
+            .unique();
+        if (existing) {
+            throw new Error(`Metadata already exists for key ${args.key}. Please use a unique key.`);
+        }
+        await ctx.db.insert("metadata", {
+            authorId: args.authorId,
+            key: args.key,
+            contentType: args.expectedContentType,
+            size: args.expectedSize,
+            uploadStatus: "pending",
+            expectedSize: args.expectedSize,
+            expectedContentType: args.expectedContentType,
+            uploadExpiresAt: args.uploadExpiresAt,
+            bucket: args.bucket,
+            lastModified: "",
+            link: "",
+        });
+        await ctx.scheduler.runAt(args.uploadExpiresAt + UPLOAD_CLEANUP_GRACE_MS, api.lib.expireUpload, {
+            key: args.key,
+            authorId: args.authorId,
+            bucket: args.bucket,
+            endpoint: args.endpoint,
+            accessKeyId: args.accessKeyId,
+            secretAccessKey: args.secretAccessKey,
+            forcePathStyle: args.forcePathStyle,
+        });
+        return null;
+    },
+});
+export const getUploadReservation = query({
+    args: {
+        bucket: v.string(),
+        key: v.string(),
+    },
+    returns: v.union(v.object({
+        authorId: v.optional(v.string()),
+        expectedSize: v.optional(v.number()),
+        expectedContentType: v.optional(v.string()),
+        uploadExpiresAt: v.optional(v.number()),
+        uploadStatus: v.optional(v.union(v.literal("pending"), v.literal("ready"))),
+    }), v.null()),
+    handler: async (ctx, args) => {
+        const metadata = await ctx.db
+            .query("metadata")
+            .withIndex("bucket_key", (q) => q.eq("bucket", args.bucket).eq("key", args.key))
+            .unique();
+        if (!metadata) {
+            return null;
+        }
+        return {
+            authorId: metadata.authorId,
+            expectedSize: metadata.expectedSize,
+            expectedContentType: metadata.expectedContentType,
+            uploadExpiresAt: metadata.uploadExpiresAt,
+            uploadStatus: metadata.uploadStatus,
+        };
+    },
+});
 export const getMetadata = query({
     args: {
         key: v.string(),
@@ -50,7 +139,7 @@ export const getMetadata = query({
             .query("metadata")
             .withIndex("bucket_key", (q) => q.eq("bucket", args.bucket).eq("key", args.key))
             .unique();
-        if (!metadata) {
+        if (!metadata || metadata.uploadStatus === "pending") {
             return null;
         }
         return {
@@ -64,6 +153,8 @@ export const listMetadata = query({
     args: {
         limit: v.optional(v.number()),
         cursor: v.optional(v.string()),
+        authorId: v.optional(v.string()),
+        keyPrefix: v.optional(v.string()),
         ...r2ConfigValidator.fields,
     },
     returns: paginationReturnValidator(v.object({
@@ -72,18 +163,47 @@ export const listMetadata = query({
         bucketLink: v.string(),
     })),
     handler: async (ctx, args) => {
-        const { limit, cursor, ...r2Config } = args;
+        const { limit, cursor, authorId, keyPrefix, ...r2Config } = args;
         const r2 = createR2Client(r2Config);
-        const results = await paginator(ctx.db, schema)
-            .query("metadata")
-            .withIndex("bucket", (q) => q.eq("bucket", r2Config.bucket))
-            .paginate({
-            numItems: limit ?? DEFAULT_LIST_LIMIT,
-            cursor: cursor ?? null,
-        });
+        const base = paginator(ctx.db, schema).query("metadata");
+        const keyPrefixUpperBound = keyPrefix ? `${keyPrefix}\uffff` : undefined;
+        const results = authorId && keyPrefix
+            ? await base
+                .withIndex("bucket_author_key", (q) => q
+                .eq("bucket", r2Config.bucket)
+                .eq("authorId", authorId)
+                .gte("key", keyPrefix)
+                .lt("key", keyPrefixUpperBound))
+                .paginate({
+                numItems: limit ?? DEFAULT_LIST_LIMIT,
+                cursor: cursor ?? null,
+            })
+            : authorId
+                ? await base
+                    .withIndex("bucket_author_key", (q) => q.eq("bucket", r2Config.bucket).eq("authorId", authorId))
+                    .paginate({
+                    numItems: limit ?? DEFAULT_LIST_LIMIT,
+                    cursor: cursor ?? null,
+                })
+                : keyPrefix
+                    ? await base
+                        .withIndex("bucket_key", (q) => q
+                        .eq("bucket", r2Config.bucket)
+                        .gte("key", keyPrefix)
+                        .lt("key", keyPrefixUpperBound))
+                        .paginate({
+                        numItems: limit ?? DEFAULT_LIST_LIMIT,
+                        cursor: cursor ?? null,
+                    })
+                    : await base
+                        .withIndex("bucket", (q) => q.eq("bucket", r2Config.bucket))
+                        .paginate({
+                        numItems: limit ?? DEFAULT_LIST_LIMIT,
+                        cursor: cursor ?? null,
+                    });
         return {
             ...results,
-            page: await asyncMap(results.page, async (doc) => ({
+            page: await asyncMap(results.page.filter((doc) => doc.uploadStatus !== "pending"), async (doc) => ({
                 ...withoutSystemFields(doc),
                 url: await getUrl(r2, r2Config.bucket, doc.key),
                 bucketLink: doc.link.replace(/\/objects\/.*$/, ""),
@@ -92,7 +212,10 @@ export const listMetadata = query({
     },
 });
 export const upsertMetadata = mutation({
-    args: schema.tables.metadata.validator.fields,
+    args: {
+        ...schema.tables.metadata.validator.fields,
+        requirePendingReservation: v.optional(v.boolean()),
+    },
     returns: v.object({
         isNew: v.boolean(),
     }),
@@ -101,21 +224,45 @@ export const upsertMetadata = mutation({
             .query("metadata")
             .withIndex("bucket_key", (q) => q.eq("bucket", args.bucket).eq("key", args.key))
             .unique();
+        if (args.requirePendingReservation) {
+            const sameCompletedUpload = existingMetadata?.uploadStatus === "ready" &&
+                existingMetadata.authorId === args.authorId;
+            if (sameCompletedUpload) {
+                return { isNew: false };
+            }
+            if (existingMetadata?.uploadStatus !== "pending" ||
+                !args.authorId ||
+                existingMetadata.authorId !== args.authorId ||
+                existingMetadata.expectedSize !== args.size ||
+                existingMetadata.expectedContentType !== args.contentType) {
+                throw new Error("Upload reservation is no longer pending");
+            }
+        }
         if (existingMetadata) {
             await ctx.db.patch("metadata", existingMetadata._id, {
+                authorId: args.authorId,
                 contentType: args.contentType,
                 size: args.size,
                 sha256: args.sha256,
                 lastModified: args.lastModified,
                 link: args.link,
+                uploadStatus: args.uploadStatus,
+                expectedSize: args.expectedSize,
+                expectedContentType: args.expectedContentType,
+                uploadExpiresAt: args.uploadExpiresAt,
             });
             return { isNew: false };
         }
         await ctx.db.insert("metadata", {
+            authorId: args.authorId,
             key: args.key,
             contentType: args.contentType,
             size: args.size,
             sha256: args.sha256,
+            uploadStatus: args.uploadStatus,
+            expectedSize: args.expectedSize,
+            expectedContentType: args.expectedContentType,
+            uploadExpiresAt: args.uploadExpiresAt,
             bucket: args.bucket,
             lastModified: args.lastModified,
             link: args.link,
@@ -127,29 +274,66 @@ export const syncMetadata = action({
     args: {
         key: v.string(),
         onComplete: v.optional(v.string()),
+        authorId: v.optional(v.string()),
+        requireReservation: v.optional(v.boolean()),
         ...r2ConfigValidator.fields,
     },
     returns: v.null(),
     handler: async (ctx, args) => {
-        const { key, ...r2Config } = args;
+        const { key, onComplete, authorId, requireReservation, ...r2Config } = args;
         const r2 = createR2Client(r2Config);
+        const reservation = await ctx.runQuery(api.lib.getUploadReservation, {
+            bucket: r2Config.bucket,
+            key,
+        });
+        if (requireReservation &&
+            (!reservation || !authorId || reservation.authorId !== authorId)) {
+            throw new Error("Upload reservation does not belong to this user");
+        }
+        if (reservation?.uploadStatus === "pending") {
+            if (!authorId || reservation.authorId !== authorId) {
+                throw new Error("Upload reservation does not belong to this user");
+            }
+            if (reservation.uploadExpiresAt !== undefined && Date.now() > reservation.uploadExpiresAt + UPLOAD_CLEANUP_GRACE_MS) {
+                const claimed = await ctx.runMutation(api.lib.deletePendingMetadata, {
+                    bucket: r2Config.bucket,
+                    key,
+                    authorId,
+                });
+                if (claimed) {
+                    await deleteR2Key(r2, r2Config.bucket, key);
+                }
+                throw new Error("Upload reservation expired");
+            }
+        }
         const command = new HeadObjectCommand({
             Bucket: r2Config.bucket,
             Key: key,
         });
         const response = await r2.send(command);
+        const finalizedSize = reservation?.uploadStatus === "pending"
+            ? reservation.expectedSize
+            : response.ContentLength;
+        const finalizedContentType = reservation?.uploadStatus === "pending"
+            ? reservation.expectedContentType
+            : response.ContentType;
         const accountId = /\/{2}([^/.]+)\./.exec(r2Config.endpoint)?.[1] ?? "";
         const link = `https://dash.cloudflare.com/${accountId}/r2/default/buckets/${r2Config.bucket}/objects/${key}/details`;
         const { isNew } = await ctx.runMutation(api.lib.upsertMetadata, {
             key,
             lastModified: response.LastModified?.toISOString() ?? "",
-            contentType: response.ContentType,
-            size: response.ContentLength,
+            contentType: finalizedContentType,
+            size: finalizedSize,
             sha256: response.ChecksumSHA256,
             bucket: r2Config.bucket,
             link,
+            authorId,
+            uploadStatus: "ready",
+            expectedSize: undefined,
+            expectedContentType: undefined,
+            uploadExpiresAt: undefined,
+            requirePendingReservation: requireReservation,
         });
-        const onComplete = args.onComplete;
         if (onComplete) {
             await ctx.runMutation(onComplete, {
                 key,
@@ -159,6 +343,47 @@ export const syncMetadata = action({
         }
     },
 });
+export const expireUpload = action({
+    args: {
+        key: v.string(),
+        authorId: v.string(),
+        ...r2ConfigValidator.fields,
+    },
+    returns: v.null(),
+    handler: async (ctx, args) => {
+        const { key, authorId, ...r2Config } = args;
+        const claimed = await ctx.runMutation(api.lib.deletePendingMetadata, {
+            bucket: r2Config.bucket,
+            key,
+            authorId,
+        });
+        if (!claimed) {
+            return null;
+        }
+        const r2 = createR2Client(r2Config);
+        await deleteR2Key(r2, r2Config.bucket, key);
+        return null;
+    },
+});
+export const deletePendingMetadata = mutation({
+    args: {
+        key: v.string(),
+        bucket: v.string(),
+        authorId: v.string(),
+    },
+    returns: v.boolean(),
+    handler: async (ctx, args) => {
+        const metadata = await ctx.db
+            .query("metadata")
+            .withIndex("bucket_key", (q) => q.eq("bucket", args.bucket).eq("key", args.key))
+            .unique();
+        if (metadata?.uploadStatus !== "pending" || metadata.authorId !== args.authorId) {
+            return false;
+        }
+        await ctx.db.delete(metadata._id);
+        return true;
+    },
+});
 export const deleteMetadata = mutation({
     args: {
         key: v.string(),
@@ -184,7 +409,7 @@ export const deleteR2Object = action({
     handler: async (ctx, args) => {
         const { key, ...r2Config } = args;
         const r2 = createR2Client(r2Config);
-        await r2.send(new DeleteObjectCommand({ Bucket: r2Config.bucket, Key: key }));
+        await deleteR2Key(r2, r2Config.bucket, key);
     },
 });
 export const deleteObject = mutation({
@@ -208,4 +433,4 @@ export const withoutSystemFields = (fields) => {
     const { _id, _creationTime, ...rest } = fields;
     return rest;
 };
-//# sourceMappingURL=lib.js.map
\ No newline at end of file
+//# sourceMappingURL=lib.js.map
diff --git a/node_modules/@convex-dev/r2/dist/component/schema.d.ts b/node_modules/@convex-dev/r2/dist/component/schema.d.ts
index 40917da..08cd5f7 100644
--- a/node_modules/@convex-dev/r2/dist/component/schema.d.ts
+++ b/node_modules/@convex-dev/r2/dist/component/schema.d.ts
@@ -3,22 +3,34 @@ declare const _default: import("convex/server").SchemaDefinition<{
         sha256?: string | undefined;
         contentType?: string | undefined;
         size?: number | undefined;
+        authorId?: string | undefined;
+        uploadStatus?: "pending" | "ready" | undefined;
+        expectedSize?: number | undefined;
+        expectedContentType?: string | undefined;
+        uploadExpiresAt?: number | undefined;
         bucket: string;
         key: string;
         lastModified: string;
         link: string;
     }, {
+        authorId: import("convex/values").VString<string | undefined, "optional">;
         key: import("convex/values").VString<string, "required">;
         sha256: import("convex/values").VString<string | undefined, "optional">;
         contentType: import("convex/values").VString<string | undefined, "optional">;
         size: import("convex/values").VFloat64<number | undefined, "optional">;
+        uploadStatus: import("convex/values").VUnion<"pending" | "ready" | undefined, [import("convex/values").VLiteral<"pending", "required">, import("convex/values").VLiteral<"ready", "required">], "optional", never>;
+        expectedSize: import("convex/values").VFloat64<number | undefined, "optional">;
+        expectedContentType: import("convex/values").VString<string | undefined, "optional">;
+        uploadExpiresAt: import("convex/values").VFloat64<number | undefined, "optional">;
         bucket: import("convex/values").VString<string, "required">;
         lastModified: import("convex/values").VString<string, "required">;
         link: import("convex/values").VString<string, "required">;
-    }, "required", "bucket" | "sha256" | "key" | "contentType" | "lastModified" | "link" | "size">, {
+    }, "required", "bucket" | "sha256" | "key" | "contentType" | "lastModified" | "link" | "size" | "authorId" | "uploadStatus" | "expectedSize" | "expectedContentType" | "uploadExpiresAt">, {
         bucket: ["bucket", "_creationTime"];
+        bucket_author: ["bucket", "authorId", "_creationTime"];
+        bucket_author_key: ["bucket", "authorId", "key", "_creationTime"];
         bucket_key: ["bucket", "key", "_creationTime"];
     }, {}, {}>;
 }, true>;
 export default _default;
-//# sourceMappingURL=schema.d.ts.map
\ No newline at end of file
+//# sourceMappingURL=schema.d.ts.map
diff --git a/node_modules/@convex-dev/r2/dist/component/schema.js b/node_modules/@convex-dev/r2/dist/component/schema.js
index d065b83..e7fa8f3 100644
--- a/node_modules/@convex-dev/r2/dist/component/schema.js
+++ b/node_modules/@convex-dev/r2/dist/component/schema.js
@@ -2,15 +2,22 @@ import { defineSchema, defineTable } from "convex/server";
 import { v } from "convex/values";
 export default defineSchema({
     metadata: defineTable({
+        authorId: v.optional(v.string()),
         key: v.string(),
         sha256: v.optional(v.string()),
         contentType: v.optional(v.string()),
         size: v.optional(v.number()),
+        uploadStatus: v.optional(v.union(v.literal("pending"), v.literal("ready"))),
+        expectedSize: v.optional(v.number()),
+        expectedContentType: v.optional(v.string()),
+        uploadExpiresAt: v.optional(v.number()),
         bucket: v.string(),
         lastModified: v.string(),
         link: v.string(),
     })
         .index("bucket", ["bucket"])
+        .index("bucket_author", ["bucket", "authorId"])
+        .index("bucket_author_key", ["bucket", "authorId", "key"])
         .index("bucket_key", ["bucket", "key"]),
 });
-//# sourceMappingURL=schema.js.map
\ No newline at end of file
+//# sourceMappingURL=schema.js.map
diff --git a/node_modules/@convex-dev/r2/dist/shared.d.ts b/node_modules/@convex-dev/r2/dist/shared.d.ts
index c5c62dc..b292c22 100644
--- a/node_modules/@convex-dev/r2/dist/shared.d.ts
+++ b/node_modules/@convex-dev/r2/dist/shared.d.ts
@@ -1,6 +1,7 @@
 import { type GenericValidator, type Infer } from "convex/values";
 import { S3Client } from "@aws-sdk/client-s3";
 export declare const r2ConfigValidator: import("convex/values").VObject<{
+    forcePathStyle?: boolean | undefined;
     bucket: string;
     endpoint: string;
     accessKeyId: string;
@@ -10,7 +11,8 @@ export declare const r2ConfigValidator: import("convex/values").VObject<{
     endpoint: import("convex/values").VString<string, "required">;
     accessKeyId: import("convex/values").VString<string, "required">;
     secretAccessKey: import("convex/values").VString<string, "required">;
-}, "required", "bucket" | "endpoint" | "accessKeyId" | "secretAccessKey">;
+    forcePathStyle: import("convex/values").VBoolean<boolean | undefined, "optional">;
+}, "required", "bucket" | "endpoint" | "accessKeyId" | "secretAccessKey" | "forcePathStyle">;
 export declare const createR2Client: (args: Infer<typeof r2ConfigValidator>) => S3Client;
 export declare const paginationReturnValidator: (docValidator: GenericValidator) => import("convex/values").VObject<{
     splitCursor?: string | null | undefined;
diff --git a/node_modules/@convex-dev/r2/dist/shared.js b/node_modules/@convex-dev/r2/dist/shared.js
index 1c7516e..a04773e 100644
--- a/node_modules/@convex-dev/r2/dist/shared.js
+++ b/node_modules/@convex-dev/r2/dist/shared.js
@@ -5,6 +5,7 @@ export const r2ConfigValidator = v.object({
     endpoint: v.string(),
     accessKeyId: v.string(),
     secretAccessKey: v.string(),
+    forcePathStyle: v.optional(v.boolean()),
 });
 export const createR2Client = (args) => {
     return new S3Client({
@@ -14,6 +15,7 @@ export const createR2Client = (args) => {
             accessKeyId: args.accessKeyId,
             secretAccessKey: args.secretAccessKey,
         },
+        forcePathStyle: args.forcePathStyle,
         // Blob bodies crash the auto-checksum path in non-Node runtimes.
         // Workaround per aws/aws-sdk-js-v3#6834.
         requestChecksumCalculation: "WHEN_REQUIRED",
